# Archived agents

These agent definitions are retained for historical reference but are not exposed through active discovery paths.

- `metis.md` encoded mandatory pre-planning analysis that is now handled proportionately by the main agent or `oracle`.
- `momus.md` encoded a separate formal plan-review gate that is no longer part of the default workflow.

